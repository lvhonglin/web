#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/F:/工作/代码/java/testone/guitar/ideplugin/web/4.javascript/15.babel转码器/node_modules/.store/@babel+cli@7.23.0/node_modules/@babel"
else
  export NODE_PATH="$NODE_PATH:$basedir/F:/工作/代码/java/testone/guitar/ideplugin/web/4.javascript/15.babel转码器/node_modules/.store/@babel+cli@7.23.0/node_modules/@babel"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../.store/@babel+cli@7.23.0/node_modules/@babel/cli/bin/babel.js" "$@"
else
  exec node  "$basedir/../.store/@babel+cli@7.23.0/node_modules/@babel/cli/bin/babel.js" "$@"
fi
